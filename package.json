{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm run --prefix ./fflags-types build && npm run --prefix ./fflags-client build && npm run --prefix ./fflags-mongo-loader build && npm run --prefix ./fflags-rest-loader build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.7.6",
    "lerna": "^8.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
