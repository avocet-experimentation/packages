{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm i && npm run --prefix ./estuary-types build && npm run --prefix ./estuary-client build"
  },
  "workspaces": [
    "*"
  ],
  "devDependencies": {
    "@types/node": "^22.7.6",
    "typescript": "~5.4.5",
    "vitest": "^2.1.3"
  }
}
